import React from "react"
import { Pre } from "../../components/pre"

export const salmonSeasonCode = () => {
  return (
    <Pre>
      <code className="language-javascript">
        {`let relationship = normal
while(relationship) {
    if (i am happier without you) {
        worryAbout(relationship)
    } else if (you are happier without me) {
        question(relationship)
    } else if (we are happy together && we are happy alone) {
        enjoy(relationship)
    }
}
          `}
      </code>
    </Pre>
  )
}
